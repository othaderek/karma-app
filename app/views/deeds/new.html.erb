<h1> Create A New Deed </h1>

<% if flash[:errors] %>
  <ul>
    <% flash[:errors].each do |err| %>
      <li>
        <%= err %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= form_for @deed do |f| %>
  <%= f.label :item %>
  <%= f.text_field :item %>
  <%= f.submit %>
<% end %>
